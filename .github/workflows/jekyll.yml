name: fooling around

on:
  push:
    branches:
      - master

jobs:
  useless_job:
    runs-on: ubuntu-latest
    steps:
    - uses: docker://mbentley/figlet
    - name: some figlet thing
      run: |
        figlet holy fig
