name: fooling around

on:
  push:
    branches:
      - master

jobs:
  useless_job:
    runs-on: ubuntu-latest
    steps:
    - uses: docker://alpine:3.8
    - name: some figlet thing
      run: |
        apk update && apk upgrade \
        apk add figlet \
        figlet holy fig
